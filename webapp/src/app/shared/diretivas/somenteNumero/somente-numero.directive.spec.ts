import { SomenteNumeroDirective } from './somente-numero.directive';

describe('SomenteNumeroDirective', () => {
  it('should create an instance', () => {
    const directive = new SomenteNumeroDirective();
    expect(directive).toBeTruthy();
  });
});
